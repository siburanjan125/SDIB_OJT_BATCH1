#include<stdio.h>
#include<string.h>

int main()
{
    char address[50];
    printf("\nEnter the address:-");
    gets(address);
    printf("\nENTERED STRING IS: ");
    puts(address);
    return 0;
}